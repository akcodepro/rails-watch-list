<div class="container">
  <h1>New List</h1>
  <div class="container">
    <%= simple_form_for @list do |f| %>
      <%= f.input :name, placeholder: 'List name' %>
      <%= f.button :submit %>
    <% end %>
  </div>
  <%= link_to "Back to all lists", lists_path %>
</div>
